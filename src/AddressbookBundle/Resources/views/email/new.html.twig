{% extends 'base.html.twig' %}

{% block body %}
    <h1>Email creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('_edit', {'id': pid}) }}">Back to person edit</a>
        </li>
    </ul>
{% endblock %}
